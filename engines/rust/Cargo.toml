[package]
name = "electrolite"
version = "0.0.1"
edition = "2021"
publish = false

[dependencies]
rusqlite = { version = "0.31", features = ["bundled", "hooks"] }
serde_json = "1"

[dev-dependencies]
tempfile = "3"
