{
  "name": "shape_handle is byte-identical across all engines",
  "setup": "INSERT INTO todos (id, project_id, title, done) VALUES (1, 'p1', 'first', 0);",
  "operations": [
    {
      "kind": "GET",
      "path": "/electrolite/v1/projectTodos/p1",
      "query": "offset=-1"
    }
  ],
  "cross_engine": ["0.body.shape_handle", "0.body.key_columns"]
}
