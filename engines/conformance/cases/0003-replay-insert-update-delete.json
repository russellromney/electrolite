{
  "name": "replay insert/update/delete messages identical across engines",
  "setup": "INSERT INTO todos (id, project_id, title, done) VALUES (1, 'p1', 'seed', 0);",
  "operations": [
    { "kind": "GET", "path": "/electrolite/v1/projectTodos/p1", "query": "offset=-1" },
    { "kind": "exec", "sql": "INSERT INTO todos (id, project_id, title, done) VALUES (?, ?, ?, 0)", "args": [10, "p1", "new"] },
    { "kind": "exec", "sql": "UPDATE todos SET title = ? WHERE id = ?", "args": ["renamed", 10] },
    { "kind": "exec", "sql": "DELETE FROM todos WHERE id = ?", "args": [10] },
    {
      "kind": "GET",
      "path": "/electrolite/v1/projectTodos/p1",
      "query": "offset={prev[0].body.offset}&log_id={prev[0].body.log_id}&shape_handle={prev[0].body.shape_handle}"
    }
  ],
  "cross_engine": [
    "0.body.shape_handle",
    "4.status",
    "4.body.type",
    "4.body.shape_handle"
  ]
}
