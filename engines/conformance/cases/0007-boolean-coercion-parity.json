{
  "name": "boolean coerced to 0/1 against BOOLEAN columns; identical handle across engines",
  "operations": [
    {
      "kind": "GET",
      "path": "/electrolite/v1/enabledFlags",
      "query": "offset=-1"
    }
  ],
  "cross_engine": [
    "0.status",
    "0.body.shape_handle",
    "0.body.key_columns"
  ]
}
